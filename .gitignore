# ---------------------------------------------------------------
# 绝对不能提交的内容
# ---------------------------------------------------------------

# 本地数据目录
#   data/db.json        达人真实姓名、手机号、收件地址、UID、合作码
#   data/settings.json  模型 API Key
data/

# 环境变量（含 API Key）
# 注意：不要写成 .env.* —— 那会连模板一起忽略，得再加否定规则，容易出错。
# 这里逐个列出，.env.example 作为模板必须提交。
.env
.env.local
.env.development
.env.production

# ---------------------------------------------------------------
# 常规忽略
# ---------------------------------------------------------------
node_modules/
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.tmp
